{% extends 'base.html.twig' %}

{% block title %}Edit Category
{% endblock %}

{% block body %}
	<div class="container">

		<div class="row justify-content-center mt-5">

			<div class="col-md-8">
				<h1>Modifier - Catégories</h1>
				{{include('category/_form.html.twig')}}
			</div>
		</div>
		<div class="row  py-5">
			<div class="col-md-6">
			{% if is_granted('ROLE_ADMIN') %}
				{{ include('category/_delete_form.html.twig') }}
			{% endif %}	
			</div>
			<div class="col-md-6">
				<a href="{{ path('category_index') }}" class="btn btn-secondary mx-5 my-2">
					<i class="fas fa-list"></i>
					Retour vers liste</a>
			</div>
		</div>
	</div>
{% endblock %}
