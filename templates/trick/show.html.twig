{% extends 'base.html.twig' %}

{% block title %}Trick
{% endblock %}

{% block body %}
<div class="container h-100">
	<div class="row align-items-center h-100">
		<div class="col-12">
			<div class="card h-100 justify-content-center" id="trick-show">
				<div>
					<h1>Trick</h1>

					<table class="table">
						<tbody>

						<!--	<tr>
								<th>Id</th>
								<td>{{ trick.id }}</td>
							</tr> -->
							<tr>
								<th>Name</th>
								<td>{{ trick.name }}</td>
							</tr>
							<tr>
								<th>Description</th>
								<td>{{ trick.description }}</td>
							</tr>
							<tr>
								<th>CreatedAt</th>
								<td>{{ trick.createdAt ? trick.createdAt|date('d/m/Y à h\\hi') : '' }}</td>
							</tr>
							<tr>
								<th>UpdateAt</th>
								<td>{{ trick.updateAt ? trick.updateAt|date('d/m/Y à h\\hi') : '' }}</td>
							</tr>
						</tbody>
					</table>
				</div>
			</div>
		</div>
	</div>
</div>

	
<div class="d-flex justify-content-center">
    <a href="{{ path('trick_index') }}">back to list</a>
</div>
	<a href="{{ path('trick_edit', {'id': trick.id}) }}">edit</a>

	{{ include('trick/_delete_form.html.twig') }}
{% endblock %}
